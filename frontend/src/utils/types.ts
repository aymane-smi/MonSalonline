export type formInputs = {
    fname: String,
    lname: String,
    email: String,
    phone: String,
    username: String,
};

export type storeState = {
    screen: Number,
    day: Number | null,
}